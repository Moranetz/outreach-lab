# Vapi example

Run `outreach-lab all vapi` against the config in `targets/vapi.yaml`.

This is one of the strongest topical fits in Marion's target list because the Atlas's technique library IS the input every Vapi customer should be using to design their voice scripts. The AE seat at Vapi is the seat where Marion's two distinct credentials (iOS shipping + Atlas research) collapse into one credential.

Target config currently has `<verify>` placeholders — fill them in with sourced specifics from primary research before generating final outputs.
